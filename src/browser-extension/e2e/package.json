{
  "name": "downloader-extension-e2e",
  "private": true,
  "description": "End-to-end tests that load the real unpacked extension in Chromium and drive real pages (see README.md).",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
