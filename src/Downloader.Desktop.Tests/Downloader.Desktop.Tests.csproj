<Project Sdk="Microsoft.NET.Sdk">
    <PropertyGroup>
        <TargetFramework>net10.0</TargetFramework>
        <Nullable>disable</Nullable>
        <IsPackable>false</IsPackable>
        <IsTestProject>true</IsTestProject>
        <!-- The app project is self-contained, so the test project must be too (matches host RID). -->
        <RuntimeIdentifier>$(NETCoreSdkPortableRuntimeIdentifier)</RuntimeIdentifier>
        <SelfContained>true</SelfContained>
    </PropertyGroup>

    <ItemGroup>
        <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.12.0" />
        <PackageReference Include="xunit.v3" Version="3.2.2" />
        <PackageReference Include="xunit.runner.visualstudio" Version="3.1.5" />
        <PackageReference Include="Avalonia.Headless.XUnit" Version="12.0.4" />
    </ItemGroup>

    <ItemGroup>
        <ProjectReference Include="..\Downloader.Desktop\Downloader.Desktop.csproj" />
        <ProjectReference Include="..\Downloader.Desktop.Plugins.Abstractions\Downloader.Desktop.Plugins.Abstractions.csproj" />
        <!-- Build the sample plugin (but DON'T reference it for compilation) so the loader integration
             test can load it from disk as a real external DLL. -->
        <ProjectReference Include="..\..\samples\Downloader.Desktop.SamplePlugin\Downloader.Desktop.SamplePlugin.csproj"
                          ReferenceOutputAssembly="false" Private="false" />
    </ItemGroup>

    <!-- Stage the built sample plugin DLL into <testout>/plugins-sample so the integration test can load it.
         The sample may land in the plain TFM folder (bin/<cfg>/net10.0/) OR a RID subfolder: this test
         project is self-contained with a RuntimeIdentifier, and a parallel solution build can propagate that
         RID to the referenced sample, so its DLL races between those two locations on CI. Glob recursively so
         we stage it wherever it lands; exclude the metadata-only ref/ assemblies (they can't be loaded). The
         old hardcoded non-RID path silently skipped the copy (Condition Exists) when the sample landed under a
         RID — a green build but a failing runtime test ("sample plugin was not staged"). The Error now turns
         any genuine miss into a loud build failure instead of a confusing test failure. -->
    <Target Name="StageSamplePlugin" AfterTargets="Build">
        <PropertyGroup>
            <SamplePluginBin>$(MSBuildThisFileDirectory)..\..\samples\Downloader.Desktop.SamplePlugin\bin\$(Configuration)\net10.0</SamplePluginBin>
        </PropertyGroup>
        <ItemGroup>
            <SamplePluginDll Include="$(SamplePluginBin)\**\Downloader.Desktop.SamplePlugin.dll"
                             Exclude="$(SamplePluginBin)\**\ref\**" />
        </ItemGroup>
        <Error Condition="'@(SamplePluginDll)' == ''"
               Text="StageSamplePlugin: Downloader.Desktop.SamplePlugin.dll not found under $(SamplePluginBin) (searched recursively, excluding ref/). The plugin loader integration test would fail. Ensure the sample plugin project built for the '$(Configuration)' configuration." />
        <Copy SourceFiles="@(SamplePluginDll)" DestinationFolder="$(OutDir)plugins-sample"
              SkipUnchangedFiles="true" />
    </Target>
</Project>
