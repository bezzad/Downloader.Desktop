# PLAN.md

This is the living plan for Downloader.Desktop. It is kept current and committed
on `develop` so that work can continue seamlessly across machines. Read this
file (and `TASKS.md`) at the start of every session before doing anything else.

**Standing rules (see `CLAUDE.md` → "Workflow & progress tracking" for full text)**:
invoke the `downloader-desktop` skill before starting; work only on `develop`;
write Clean Code/KISS — simplest solution, no speculative abstractions; on
failure, mark `[!]` in Blocked/Failed with the reason and commit+push immediately
so any machine/AI picks up the true last state.

**Last updated**: 2026-06-22
**Branch**: develop
**Now working on**: (idle) — **Released v1.4.0** (accent themes, language flags, redesigned updater + many fixes) to all channels. See Done.

Author decisions (2026-06-19 overnight batch): may cut releases autonomously; Issue 1 = just fix the false alarm (don't touch update/download mechanism); Queues = full "real queue manager" redesign (mockup approved); Instagram = R&D plan only, no code.

## Status legend
- `[ ]` todo
- `[~]` in progress
- `[x]` done
- `[!]` blocked / failed

## Active
- (none)

## Todo
- [ ] (author, when awake) Interactively launch `/Applications/Downloader.app` (Spotlight/double-click) to eyeball the new Queues page. Note: a real GUI launch is expected to work — the only failure seen was an `Avalonia.Native` render-timer error (-6661) when launching from this background/headless shell, which happens at platform bootstrap BEFORE any app code and is purely "no window-server session", not a code bug. Verified instead via build + 66 tests (incl. real-App headless render of Queues) + screenshots.
- [ ] **Video-site downloads (HLS/.m3u8 — YouTube/Instagram/etc.) — DEFERRED, direction LOCKED.** Plan: `docs/instagram-rnd.md` (restored 2026-06-22 after it was deleted in `1105594`). Decisions locked by the author (2026-06-22): **full Option A** = yt-dlp + ffmpeg behind an `IMediaExtractor` (multi-site, not IG-only); **download binaries on first use** (don't bundle); **generic "video sites" framing** + personal-use disclaimer; **ship = not now**. When picked up: detect page-URL vs direct file → `yt-dlp -J` → route progressive MP4 through our multipart engine, let yt-dlp+ffmpeg handle HLS/DASH/split-AV; mock-extractor tests. (Supersedes the old "Issue 3 — IG R&D plan only" item, which is done.)
## Done
- [x] **Released v1.4.0 — accent themes, language flags, redesigned updater + many fixes.** Bumped 1.3.3→1.4.0, merged develop→main, tagged `v1.4.0`. **CI hiccup (fixed for next time):** the `generate_release_notes: true` I'd added to `release.yml`'s 4-way matrix `action-gh-release` made release creation race → `tag_name already_exists`, so the **osx-x64** job failed to upload its asset and `release.sh` timed out at the macOS-wait. Recovered by building osx-x64 locally (no single-file, `make-macos-app.sh`) and `gh release upload`; then finished the skipped steps by hand: curated Markdown notes set, Homebrew tap + mirror → 1.4.0 (arm `5e684564…`, intel `465d95f3…`), winget mirror → 1.4.0 + PR **microsoft/winget-pkgs#391334** (closed the superseded 1.3.3 PR #391296). **Permanent fix** (this commit): removed `generate_release_notes` from the matrix (and snap.yml); added a single post-build `notes` job in `release.yml` that fills auto-notes only if the body is empty (no race, never clobbers curated notes). Snap auto-published (rev up). All 5 assets present.
- [x] **Released v1.3.3 — fixed two Linux install issues.** (1) **curl installer 504**: `scripts/install.sh` called the unauthenticated `api.github.com/releases/latest` (rate-limited 60/hr per IP → intermittent `curl: (22) … 504` at "Finding the latest release"). Switched to the API-free `https://github.com/<repo>/releases/latest/download/<asset>` CDN redirect + `curl --retry 5` (transient 504/503/timeout now retry). Verified end-to-end into a sandbox HOME. (`fdcba06`) (2) **Snap installed but wouldn't open**: `snap run downloader` → `Failure processing application bundle. Failed to create directory [~/.cache/dotnet_bundle_extract/…] Error code: 13`. The single-file build self-extracts native libs (libSkiaSharp.so …) to `~/.cache` by default, but strict confinement's `home` interface denies hidden dot-dirs → EACCES before any window. Fix: `apps.downloader.environment.DOTNET_BUNDLE_EXTRACT_BASE_DIR=$SNAP_USER_COMMON/.dotnet_bundle_extract` (always writable; snapd expands the var). Validated locally that the env var redirects extraction. (`b289fd8`) Released via `scripts/release.sh 1.3.3`: tag `v1.3.3` (Release workflow success, 4 assets), installer fix now on `main` (the curl one-liner reads `main`), snap rebuilt+republished to the Store via `snap.yml`, Homebrew cask → 1.3.3, mirror synced.
- [x] **Released v1.3.2 + added `scripts/release.sh` (one-shot release routine).** Changes since v1.3.1: snap Store-icon fix (top-level `icon:` key in `snap/snapcraft.yaml`), `scripts/uninstall.sh` for the Linux curl install, snap publish bookkeeping. New **`scripts/release.sh [version] [--yes]`** drives the whole routine: prompts for the version (suggests next patch), bumps `VersionPrefix` + `snap/local/VERSION` on `develop`, merges `develop`→`main`, tags `vX.Y.Z` (triggers `release.yml` for GitHub assets+curl and `snap.yml` for the Snap Store), waits for the macOS archives, then rewrites the Homebrew cask in `bezzad/homebrew-tap` + the in-repo mirror with the new version+sha256. Ran it for **v1.3.2**: `main` merge `c641d42`, tag `v1.3.2` (Release workflow success, all 4 assets), tap cask → 1.3.2 (arm `898cdc35…`, intel `d125cfc1…`), mirror synced on develop. Snap Store auto-publishes via `snap.yml` (CI). curl installer serves it automatically (reads `latest`).
- [x] **Published the Snap — `sudo snap install downloader` now works.** First version live on the Snap Store: **`downloader` `latest/stable: 1.3.1` (revision 1)**, https://snapcraft.io/downloader (publisher: bezzad). Name was already registered (public). Local `snapcraft pack --destructive-mode` does NOT work on this host (Ubuntu **26.04/resolute**, while the snap targets `base: core22`/22.04 — destructive mode pulls stage-packages `libicu70`/`libssl3` from the host archive, which 26.04 lacks → needs LXD/multipass or CI's clean core22 env). Instead: downloaded the **CI-built** `downloader_1.3.1_amd64.snap` artifact (from the v1.3.1 Snap workflow run, which builds correctly) and `snapcraft upload --release=stable` with the existing local login. **CI fix committed**: the v1.3.1 Snap run built+uploaded the artifact but FAILED at "Attach snap to the GitHub Release" (`Resource not accessible by integration`), which stopped the job before "Publish to the Snap Store" → never reached the store. `snap.yml` now has `continue-on-error: true` on the attach step + `always()` on the publish step, so future tag releases auto-publish even if the release-attach errors (SNAPCRAFT_STORE_CREDENTIALS secret is already set). Also gitignored `snap-creds.txt`/`*.snap`/`parts|stage|prime`.
- [x] **Released v1.3.1** (develop merged to `main` = `b4511ef`, tag **`v1.3.1`** → all 4 release assets built). Fixes the **macOS Apple-Silicon crash on Start download** (compressed single-file bundle faulted inflating a managed assembly → SIGABRT) by publishing macOS as a normal self-contained `.app` (no single-file/compression; win/linux unchanged); also picks up Microsoft.Extensions 10.0.9. **Homebrew tap** → 1.3.1 + real macOS sha256 (tap commit `b30bc60`); in-repo mirror synced.
- [x] **Fix: macOS crash on Start download (SIGABRT in inflate)** — root cause: compressed single-file bundle (`EnableCompressionInSingleFile`) faults on Apple Silicon inflating the first managed assembly (Data Abort → FailFast → abort). Fix: macOS publishes without single-file/compression (loose files in the .app); `release.yml` + `scripts/publish.sh` (osx-only). Shipped in v1.3.1.
- [x] **Round 17 — CI fix, auto-update rework, single-instance, snap, extension publish, queue fixes** (develop, 102 tests green):
  - **CI flake** fixed: moved `Chunk_status_text_tracks_progress` to an `[AvaloniaFact]` (it read `Localizer` as a plain `[Fact]` → got raw i18n keys depending on test order → intermittent macOS Test-step failures).
  - **Auto-update reworked** (Telegram-style), fixing the hang where the app couldn't restart and the close button did nothing: `UpdateFlow` auto-downloads in the background, shows an "Update Downloader" button at the bottom of the nav rail + a **native** system notification, and applies the staged swap **on exit** (button-quit OR manual close both install; close-to-tray bypassed when ready). Settings shows download progress → "Restart to update". All state marshaled to UI (old bug: quit called from a background continuation). Disabled under snap.
  - **Single instance** (`SingleInstanceService`): loopback lock-port; a 2nd launch forwards its URL to the primary, which surfaces the window + adds the link, then exits.
  - **Snap packaging**: `snap/snapcraft.yaml` + `snap/gui` + `scripts/build-snap.sh` + `.github/workflows/snap.yml`; README Snap section. **Author publishes** (`snapcraft login`).
  - **Browser-extension publish prep**: `scripts/build-extension.sh` (Chrome/Edge + Firefox zips), `PUBLISHING.md`, `PRIVACY.md`, README section. **Author submits** to the stores (needs dev accounts).
  - **Queue page**: fixed scroll-to-end (last row was unreachable) + width overflow behind the window when narrow. **Stop Queue** now stops (cancels) all items, not just pauses running ones.
- [x] **Released v1.3.0** (develop merged to `main` = merge `9edf98a`, tag **`v1.3.0`** → `release.yml` built win/linux/macOS×2 + GitHub Release with all 4 assets). Contents: Rounds 15+16 + fixes — browser integration (listener + Chrome/Edge/Firefox MV3 extension), About dialog + Donate, shutdown-on-completion (cancelable dialog + native notice), queue Start/Stop + Stop-all toolbar, granular notifications, **7 new languages → 16 total**, Time Left column, tri-state select-all, square selection-gated toolbar, progress-only state coloring, "file already exists"→completed, copyable URL/error in details, live "% filename" details title, Linux install icon, paste-links placeholder. Engine Downloader 5.9.0; 101 tests. **Homebrew tap** `bezzad/homebrew-tap` updated to 1.3.0 + real macOS sha256 (commit `05b25cf`); in-repo `Casks/downloader.rb` mirror synced.
- [x] **Details window: title shows "{percent}% {name}", removed duplicate header** (develop, 101 tests green). The window/title-bar caption is now a live `WindowTitle` ("21% movie.mkv") next to the app icon; dropped the redundant filename header row (the URL is already in section one) and shifted all sections up. Verified via the details screenshot.
- [x] **Fixes: Start-queue + completed-progress** (develop, 101 tests green). (1) **"Start queue → <name>" did nothing** when its rows were Stopped/Failed (e.g. after Stop/Stop-all) — `PumpQueue` only resumes Paused/Created/None, so `StartQueue` now re-queues Stopped/Failed → Created before pumping. (2) **Completed items showed a 0% bar** (files that already existed on disk have `Downloaded=0`, and it recomputed from bytes esp. after restart) — completed rows now always render 100% (ctor + `Status` setter force it; already-exists handler persists `Downloaded`). +3 tests. Also added the gated real-app shutdown verification (`ShutdownVerificationTests`, DLDESKTOP_VERIFY=1) — confirmed the real flow pops the dialog, Cancel works, no power-off.
- [x] **Shutdown-on-completion fixes** (develop, 96 tests green): (1) **Stop All no longer triggers shutdown** — the all-complete/shutdown trigger now fires only when a download actually *completed* (`FinishTerminal` calls `MaybeAllCompleted` only if `vm.Status==Completed`), not when the user cancels rows; +regression test. (2)(3) **Cancel is now visible even from the tray** — replaced the in-app toast with a Topmost `ShutdownView` countdown dialog (Cancel / Shut down now / Esc) that shows even when the window is hidden to tray, plus a **native OS notification** heads-up (`NotifyOnShutdown`). Keys `Shutdown_*` in all 16 locales. Verified the dialog via a temporary screenshot.
- [x] **Details window: copyable error message** (develop, 95 tests green). The failure banner in `DownloadDetailsView` now shows the error with a **copy button** (CopyRegular → Checkmark + "Error copied!" for 2s, `CopyErrorCommand`/`ErrorCopied`/`CopyErrorTooltip` mirroring the copy-path pattern) and the message is a `SelectableTextBlock` for partial copies — so users can paste it into a GitHub issue. `Action_CopyError`/`Action_ErrorCopied` keys in all 16 locales. Verified via a (temporary) failed-item details screenshot.
- [x] **Fix: "file already exists" no longer shows as Failed** (develop, 95 tests green). With FileExistPolicy=IgnoreDownload (app default), re-adding a file that's already on disk made the engine skip it (`DownloadFileCompleted` Cancelled=true/Error=null, no DownloadStarted) → the app wrongly reported "connection lost/timed out → Failed". Now `DownloadManager.TryMarkAlreadyExists` detects it (`LooksAlreadyDownloaded(policy, path)` + the final path from `e.UserState`/`Package.FileName`), backfills name/size, and marks the row **Completed** with `AlreadyExisted=true` → StatusText shows localized **"Already downloaded"** (`State_Exists`, all 16 locales). +2 tests.
- [x] **Round 16 — UI batch + browser extension** (develop, 92 tests green). (1) Distinct queue Start/Stop icons (`PlayStack`/`StopStack`, vs single-item play/stop). (2) Top-bar **Donate (♥)** → repo `Donate.md` (Tether BEP20/TRC20 + Liberapay, from sibling Downloader README). (3) **About dialog** (`AboutView`/`AboutViewModel` via `DialogHelper.ShowAbout`): logo+title+version+donate+website left; open-source/engine/translate sections + GitHub/Telegram(@bezzad)/Email(behzad.khosravifar@gmail.com) right — original layout, not a clone. (4) **Time Left** column + `TimeLeftText`/`FormatDuration` ETA. (5) **Select-all moved to grid header** as a tri-state checkbox (`DownloadsViewModel.SelectAllState`), removed from toolbar. (6) **Square toolbar buttons** (icon-top/text-bottom, `Button.tool`); Start/Pause/Stop/Remove gated on selection (disabled when none checked, via `HasSelection` canExecute); Stop-All/Start-Queue/Stop-Queue always enabled. (7) On state change only the **progress bar recolors** + "%· state" under it; name/status keep one style (dropped failed/pending name classes + badge). (8) README: report bugs via **GitHub Issues**, not Telegram/email. (9) **Browser extension** `src/browser-extension/` (Chrome/Edge + Firefox MV3): context menu, popup (paste/scan/detected media + send all), video/audio/HLS `.m3u8` capture w/ badge → app listener (`/add`, `/ping`); YouTube/DRM unsupported; **Safari + store deploy deferred to author**. New icons (Heart/Info/Github/Send/Mail/Globe/PlayStack/StopStack); About/contact/time-left i18n keys across all 16 locales. Skill updated with patterns.
- [x] **Round 15 — 9-item feature batch** (develop). (1) **Shutdown system on completion** — persistent Setting; `ShutdownService` powers off 30 s after all downloads finish with a cancelable in-app notice; manager raises `AllDownloadsCompleted` (UI/OS-free), MainViewModel handles notify+shutdown. (2)(3) **Start queue / Stop queue** dropdown buttons in the downloads toolbar (`QueueActionTarget` MenuFlyout, one entry per queue). (4) **Stop all** button — `StopAll` now cancels every running/paused/queued item. (5) **Granular notifications** — complete / failed / all-complete / shutdown toggles under the master switch (NOTIFICATIONS card). (6) **7 new languages** — Hindi, Italian, Portuguese(BR), Russian, Chinese(Hans), Japanese, Korean (full packs) + new keys added to all 16 locales. (7) **Browser integration** — opt-in localhost `HttpListener` (127.0.0.1:15151) that opens Add pre-filled; **app side only, extension TBD**. (8) Paste placeholder → "Paste download links…". (9) **Linux icon fix** — install.sh fetches/installs the PNG into hicolor+pixmaps and refreshes the cache; publish.sh/release.yml bundle it in the tarball. Build clean, **83 tests green**, app launches clean. Skill updated with the patterns. Commits on develop. Cause: `NotificationService` posted via `osascript "display notification"`, which macOS attributes to Script Editor (so its icon shows; `display notification` can't set an icon). Fix: new `Services/MacNotifier.cs` posts in-process via the Obj-C runtime (NSUserNotification, P/Invoke into libobjc — no NuGet/workload dependency, AOT-safe), so the banner adopts the app bundle's own `downloader.icns`. Removed the osascript path + now-unused `Escape`; falls back to the in-app toast on failure. Build clean (0 warnings), 66 tests green; confirmed NSUserNotification/NSUserNotificationCenter classes resolve on this macOS. Visual confirmation needs a GUI launch of `Downloader.app` (headless shell can't, per the -6661 note). Ships in the next release.
- [x] **Released v1.2.0** (develop merged to `main`, tag `v1.2.0` → `release.yml` builds win/linux/macOS×2 + GitHub Release). Contents: engine **Downloader 5.9.0**; **name + size preview** before start via `RemoteFileResolver`; **queued names cached across restarts** (`DownloadItem.PreviewName`); **per-connection progress %** column in details; **"Remember last save folder"** setting (default on); **Turkish/Azerbaijani/German** localizations; fix **spurious "connection timed out"** (per-block `BlockTimeout` 1s→5s + retries widen it + migrate old configs + honest detail state) and **macOS window resize** (manual resize replacing the no-op `BeginResizeDrag` on borderless windows). 66 tests green. NOTE: the matching `Downloader` *package* timeout fix lives in the sibling `../Downloader` repo and needs its own 5.9.1 package release — the desktop ships the fix via its own `BlockTimeout=5000` setting regardless.
- [x] **Use the engine's new `RemoteFileResolver` for the downloads grid (filename + size without downloading).** Done in v1.2.0: bumped `Downloader` to **5.9.0**, replaced hand-rolled `UrlResolver.ResolveFileNameAsync` with `RemoteFileResolver.GetFileInfoAsync` (kept the concurrency gate + timeout), and the grid now previews **name and size** for queued items.
- [x] **Issue 3 — Instagram video download R&D (plan only).** Delivered `docs/instagram-rnd.md`: IG links are HTML pages, not files; the engine only fetches direct HTTP+range URLs, so a media extractor is needed. Evaluated yt-dlp-bundling (recommended, also unlocks YouTube/TikTok/HLS — the deferred roadmap item) vs .NET libs (none robust) vs official API (doesn't expose arbitrary public videos) vs DIY scraper (fragile). Flagged the Meta-ToS/legal angle (app is published under author's name). Not simple → no code, per author's "plan only". Decision points listed for the author. Commit pending below.
- [x] **Issue 2 — Queues page redesigned into a real queue manager.** Was static cards (names + badges). Now: per-queue card with live aggregate stats (downloading/waiting/done/failed + total speed), combined progress bar, run/pause toggle, concurrency cap, and the queue's downloads with per-item progress + pause/resume/retry/cancel/remove + reorder (up/down priority) + move-between-queues menu (shown only when >1 queue). Added `MoveToQueue`/`MovePriority` to the manager; `Initialize` backfills `QueueId` for legacy items; `QueueItemViewModel`/`QueueMoveTarget` wrappers; 3 new icons + i18n keys; +2 tests (66 green); queues-dark/light screenshots. Commit 92234e9. Verified both themes via screenshot capture. Released as v1.1.3 (see Todo to push to brew). Detail in SKILL.md.
- [x] **Issue 1 — fixed false "Update available" on the latest version.** Root cause: `AssemblyVersion` was pinned to `$(VersionPrefix).0.0` = `1.1.0.0`, so the app always reported `1.1.0` and saw the `v1.1.1` release as newer forever; About also showed the 4-part `InformationalVersion`. Fix: `VersionPrefix` is now the full 3-part semver (`1.1.2`), `AssemblyVersion=$(VersionPrefix).0` (real patch), `release.yml` stamps `-p:VersionPrefix=<tag-without-v>` so a build always reports its tag, and About (`SettingViewModel.AppVersion`) shows `UpdateService.CurrentVersion` so About+check+tag agree. +1 regression test (app must not flag its own release). Commit f7056d7; cut **release v1.1.2** (CI stamped `1.1.2` into the bundle, verified); tap cask → 1.1.2 + real SHAs. **Verified live**: `brew upgrade` 1.1.1→1.1.2, `/Applications/Downloader.app` reports `1.1.2`. Scope per author: false-alarm only; update download/apply mechanism unchanged (note: the self-updater would still mis-handle the new `.app` layout, but with versions matching it won't be triggered — left for a later round).
- [x] **Fixed macOS app invisible in Spotlight + closing when terminal closes.** Root cause: the Homebrew cask installed a bare Unix binary (`binary "Downloader"`) — macOS never indexes it and it runs as a foreground terminal child. Fix: ship a real `Downloader.app` bundle. Added `scripts/make-macos-app.sh` (wraps the self-contained binary + Info.plist + icns), wired into `release.yml` + `publish.sh` for `osx-*`, cask → `app "Downloader.app"`, README/CONTRIBUTING updated — c602e17. Cut **release v1.1.1** (CI built all 4 platforms, macOS as `.app`), filled the cask's real per-arch sha256, pushed to `bezzad/homebrew-tap` (commit 652c1c4). **Verified live on this Mac**: `brew install --cask downloader` → `/Applications/Downloader.app`, found by Spotlight (`mdfind`) + LaunchServices, launches detached (parent PID 1). cask sha commit on develop — see below.
- [x] Set up cross-machine task tracking: PLAN.md, TASKS.md, CLAUDE.md workflow section — 53ec993
- [x] Remove private full-name path from settings screenshots: sanitized sample `DefaultSavePath` + de-hardcoded screenshot `OutDir` in `CaptureScreenshots.cs`, regenerated all 7 PNGs — 4dc44b2. Note: the old string remains in git history on all branches (already public on GitHub) — author chose to leave history as-is rather than rewrite/force-push.
- [x] Codified permanent standing rules in CLAUDE.md (Clean Code/KISS, invoke `downloader-desktop` skill before starting, always record failures in PLAN/TASKS for cross-machine visibility); resolved the resulting conflict with the old "never commit automatically" line; added pointers in PLAN.md/TASKS.md headers — 1ef9a1a
- [x] Diagnosed + fixed non-working winget/Homebrew install commands: confirmed `bezzad/homebrew-tap` (404) and the winget-pkgs manifest (404) were never actually published; corrected README to stop presenting them as ready and explain why; filled in real version+sha256 (was `1.0.0`/placeholder) in `Casks/downloader.rb` + `packaging/winget/*.yaml` from the real v1.1.0 release assets — 588f505.
- [x] **Published the Homebrew tap — `brew install --cask downloader` now works.** Created public repo `github.com/bezzad/homebrew-tap` via `gh`, pushed `Casks/downloader.rb` (v1.1.0 + real per-arch SHA) + README there. **Verified end-to-end on this Mac**: `brew tap bezzad/tap` → `brew install --cask downloader` → real arm64 Mach-O binary linked at `/opt/homebrew/bin/Downloader` (then uninstalled the test). Note: newer Homebrew requires `brew trust bezzad/tap` before install — documented in both READMEs. Main-repo README restored the working `brew` command + trust note — bec765f.

## Blocked/Failed
- (none)

## Waiting on external review
- [~] winget `bezzad.Downloader` v1.1.0 — **PR microsoft/winget-pkgs#390226**, CLA signed (`license/cla: SUCCESS`). Now in Microsoft's automated validation (downloads installer, verifies SHA256, Windows-sandbox install test) + moderator review — entirely on their side, can take hours–days. **`winget install downloader` starts working on Windows only once this PR is merged.** If validation fails, check the PR's Azure pipeline link / labels for the reason and push a fix to fork branch `bezzad:bezzad.Downloader-1.1.0`. Manifests also kept in-repo at `packaging/winget/` for the next version bump.
