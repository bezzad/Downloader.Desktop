<?xml version="1.0" encoding="utf-8"?>
<!-- MSIX package manifest for Downloader (Windows). The build script substitutes {VERSION} with the
     4-part version (e.g. 2.1.0.0). Publisher must match the signing certificate's subject exactly;
     for self-signed sideload builds that's CN=bezzad. For a Microsoft Store submission, replace
     Identity/@Name, Identity/@Publisher and Properties/PublisherDisplayName with the values reserved
     in Partner Center (see packaging/msix/README.md). -->
<Package
  xmlns="http://schemas.microsoft.com/appx/manifest/foundation/windows10"
  xmlns:uap="http://schemas.microsoft.com/appx/manifest/uap/windows10"
  xmlns:rescap="http://schemas.microsoft.com/appx/manifest/foundation/windows10/restrictedcapabilities">

  <Identity
    Name="bezzad.Downloader"
    Publisher="CN=bezzad"
    Version="{VERSION}"
    ProcessorArchitecture="x64" />

  <Properties>
    <DisplayName>Downloader</DisplayName>
    <PublisherDisplayName>bezzad</PublisherDisplayName>
    <Logo>Assets\StoreLogo.png</Logo>
    <Description>Fast multi-connection download manager with queues, scheduler and browser integration.</Description>
  </Properties>

  <Dependencies>
    <TargetDeviceFamily Name="Windows.Desktop" MinVersion="10.0.17763.0" MaxVersionTested="10.0.22621.0" />
  </Dependencies>

  <Resources>
    <Resource Language="en-us" />
  </Resources>

  <Applications>
    <Application Id="Downloader" Executable="Downloader.exe" EntryPoint="Windows.FullTrustApplication">
      <uap:VisualElements
        DisplayName="Downloader"
        Description="Fast multi-connection download manager with queues, scheduler and browser integration."
        BackgroundColor="transparent"
        Square150x150Logo="Assets\Square150x150Logo.png"
        Square44x44Logo="Assets\Square44x44Logo.png">
        <uap:DefaultTile
          Wide310x150Logo="Assets\Wide310x150Logo.png"
          Square71x71Logo="Assets\Square71x71Logo.png"
          Square310x310Logo="Assets\Square310x310Logo.png" />
        <uap:SplashScreen Image="Assets\SplashScreen.png" />
      </uap:VisualElements>
    </Application>
  </Applications>

  <Capabilities>
    <!-- Full-trust desktop app: it needs to read/write the user's files (downloads) and the network. -->
    <rescap:Capability Name="runFullTrust" />
    <Capability Name="internetClient" />
  </Capabilities>
</Package>
